spring:
  docker:
    compose:
      lifecycle-management: start-and-stop
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  cloud:
    aws:
      dynamodb:
        endpoint: http://localhost:8000
      sqs:
        endpoint: http://localhost:4566
      sns:
        endpoint: http://localhost:4566
